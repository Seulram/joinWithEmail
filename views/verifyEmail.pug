extends layouts/main

block content
    .title 회원가입
    .message 메일인증
    .message 입력하신 #{email}(으)로 이메일 인증요청이 발송되었습니다.
    form(action="/", method="get")
        input(type="submit", class="btn", value="확인")
    .sign__up
        p.sign__up-text 인증메일을 받지 못하셨나요?          
        a.sign__up-a(href=`/resendEmail`) 인증메일 재전송
    .resend-email
        .email-text 인증메일이 재전송 되었습니다